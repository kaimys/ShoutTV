{
  "name": "shouttv",
  "version": "0.0.1",
  "description": "Promote Periscope Streams over HbbTV for User Comments",
  "main": "server/init.js",
  "scripts": {
    "start": "node server/init.js & npm run watch & wait",
    "streams": "node server/streams.js",
    "build": "mkdir -p public/css public/js && browserify assets/js/app.js -v -t babelify -o public/js/app.js && stylus assets/css/style.styl -o public/css/style.css || exit 1",
    "watch": "npm run watch:js & npm run watch:css & wait",
    "watch:js": "mkdir -p public/js && watchify assets/js/app.js -v -t babelify -o public/js/app.js",
    "watch:css": "mkdir -p public/css && stylus assets/css/style.styl -o public/css/style.css"
  },
  "devDependencies": {
    "typings": "^0.7.9",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "babelify": "^7.2.0",
    "express": "^4.13.4",
    "jquery": "^2.2.2",
    "socket.io": "^1.4.5",
    "socket.io-client": "^1.4.5",
    "stylus": "^0.54.2",
    "twitter": "^1.2.5"
  }
}
